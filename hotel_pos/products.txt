/* Products component styles - blue scheme */
.products-container {
  padding: 1rem;
  font-family: 'Manrope', system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial;
}

.products-title {
  font-size: 1.25rem;
  font-weight: 700;
  color: #0b4f8a; /* deep blue */
  margin-bottom: 0.75rem;
}

.product-form .form-row {
  display: flex;
  gap: 0.5rem;
  align-items: center;
}

.input {
  padding: 0.5rem 0.75rem;
  border-radius: 0.5rem;
  border: 1px solid rgba(11,79,138,0.12);
  background: rgba(11,79,138,0.03);
  flex: 1;
}

.input-small { width: 6rem; }

.form-row-top { display:flex; gap:1rem; align-items:flex-start; }
.input-group { flex:1; display:flex; gap:0.5rem; flex-direction:column; }
.input-actions { display:flex; gap:0.5rem; align-items:center; }

.edit-panel { display:flex; flex-direction:column; gap:0.5rem; }
.edit-actions { display:flex; gap:0.5rem; }

.btn { padding: 0.5rem 0.75rem; border-radius: 0.5rem; cursor: pointer; border: none; }
.btn-primary { background: #1161b6; color: white; }
.btn-secondary { background: #e6eef9; color: #1161b6; border: 1px solid rgba(17,97,182,0.08); }
.btn-outline { background: transparent; color: #1161b6; border: 1px solid rgba(17,97,182,0.12); padding: 0.35rem 0.5rem; border-radius: 0.375rem; }
.btn-danger { background: #ffebe9; color: #b3342d; border: 1px solid rgba(179,52,45,0.08); }
.icon-btn { background: rgba(17,97,182,0.08); color: #0b4f8a; width: 2rem; height: 2rem; border-radius: 0.375rem; border: none; }

.product-list { margin-top: 1rem; }
.product-grid { display:grid; grid-template-columns: repeat(auto-fill, minmax(220px, 1fr)); gap:1rem; }
.product-card { background: linear-gradient(180deg, rgba(255,255,255,0.8), rgba(245,249,255,0.9)); border: 1px solid rgba(11,79,138,0.06); border-radius: 0.75rem; padding: 0.75rem; display:flex; flex-direction:column; gap:0.5rem; box-shadow: 0 6px 18px rgba(11,79,138,0.04); }
.product-thumb { width:100%; height:120px; display:flex; align-items:center; justify-content:center; overflow:hidden; border-radius:0.5rem; background:linear-gradient(135deg,#eef6ff,#f8fbff); }
.product-thumb img { width:100%; height:100%; object-fit:cover; }
.placeholder { font-size:1.75rem; color:#0b4f8a; font-weight:700; }
.product-body { display:flex; flex-direction:column; gap:0.25rem; }
.product-name { font-weight:700; color:#073762; font-size:1rem; }
.product-meta { display:flex; gap:0.5rem; align-items:center; }
.badge { background:#eef6ff; color:#0b4f8a; padding:0.25rem 0.5rem; border-radius:999px; font-weight:700; font-size:0.85rem; }
.product-controls { display:flex; justify-content:space-between; align-items:center; gap:0.5rem; }
.qty-controls { display:flex; align-items:center; gap:0.5rem; }
.qty-value { min-width:2rem; text-align:center; font-weight:700; }
.action-buttons { display:flex; gap:0.5rem; }

.empty { color: #6b7280; padding: 1rem; }

.edit-row { display: flex; gap: 0.5rem; align-items: center; width: 100%; }

/* small responsive tweaks */
@media (max-width: 640px) {
  .input-small { width: 4.5rem; }
  .product-grid { grid-template-columns: repeat(1, 1fr); }
  .product-thumb { height: 160px; }
}

/* Modal styles */
.modal-backdrop { position: fixed; inset: 0; background: rgba(2,6,23,0.6); display:flex; align-items:center; justify-content:center; z-index:60; }
.modal { background: linear-gradient(180deg, rgba(8,31,63,0.95), rgba(4,22,48,0.95)); color: #fff; padding: 1.25rem; border-radius: 0.75rem; width: 100%; max-width: 520px; box-shadow: 0 12px 30px rgba(2,6,23,0.6); }
.modal h4 { margin:0 0 0.5rem 0; font-size:1.1rem; color:#cfe9ff; }
.modal-form { display:flex; flex-direction:column; gap:0.5rem; }
.modal .input { background: rgba(255,255,255,0.06); border: 1px solid rgba(255,255,255,0.06); color: #fff; }
.modal .btn-primary { background: linear-gradient(90deg,#2b7bd7,#6c4fd6); }
.modal .btn-secondary { background: transparent; color: #cfe9ff; border: 1px solid rgba(255,255,255,0.08); }
/* styles removed — migrated to Tailwind utilities in the component */
